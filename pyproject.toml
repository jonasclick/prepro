[project]
name = "prepro"
version = "0.1.0"
dependencies = [
    "pyperclip",
]

[project.scripts]
prepro = "prepro:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"